import Link from 'next/link'
import categoryData from '../static/data/categoryData.js'

const linkStyle = {
  // needs styling
  marginRight: 15
}

class SearchBar extends React.Component {
  constructor(props) {
    super(props)
  }

  render() {
    return(
        <div>
        	{/*icon*/}
        {/*text field*/}
      {/*onclick call api and redirect*/}
      Searchbar
        </div>
      );
  }
}

export default SearchBar
